# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/find_this_fit

# Embedding Provider: 'openai' or 'clip'
EMBEDDING_PROVIDER=clip

# OpenAI (only if EMBEDDING_PROVIDER=openai)
OPENAI_API_KEY=sk-your-key-here
OPENAI_EMBEDDING_MODEL=image-embedding-3-large

# API Configuration
REQUEST_TIMEOUT=20

# Production settings (optional)
LOG_LEVEL=INFO
CORS_ORIGINS=*
